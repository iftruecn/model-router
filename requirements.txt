# Core dependencies — pinned to secure minimum versions
# See pyproject.toml for full security notes
fastapi>=0.115.0,<1.0.0
httpx>=0.27.0,<1.0.0
PyYAML>=6.0.2,<7.0.0
starlette>=0.40.0,<1.0.0

# Server (platform-specific)
uvicorn>=0.30.0,<1.0.0
# uvloop>=0.19.0    # Linux/macOS only
# winloop>=0.1.0    # Windows only

# Dev/Test
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
pytest-timeout>=2.3.0,<3.0.0

# Security audit (optional)
# pip-audit>=2.6.0
# safety>=3.0.0
# bandit>=1.7.0